import { Routes } from "@angular/router";
import { LoginInterfaceComponent } from "../features/login-interface/login-interface.component";

export const loginRoutes: Routes = [
    {
        path: '', 
        component: LoginInterfaceComponent,
    }
];

